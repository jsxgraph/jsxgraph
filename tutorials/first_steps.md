- Create a board with 
  - `JXG.board(BOARDID, attributes)`, see {@link JXG.board} 
  - __Attention__: we have to refer with `.` like `link JXG.board` to namespace subobjects
  - `JXG.appBox(BOARDID, attributes)`, see {@link JXG.appBox}
- See {@link JXG.Board} for the list of attributes.
  - Another link: {@link JXG.Board#animationDelay}
- Create a line like in {@link Line}
  - just a test: {@link JXG.Line#getSlope}
- MathJax
  1. Test: $\int_{-1}^1$
  1. Test: $$\int_{-1}^1$$
  1. Test: \\(\\int_{-1}^1\\)
  1. Test: \\(\int_{-1}^1\\)
  1. Test: \\[\int_{-1}^1\\]

  
  